import styled from 'styled-components';

export const Container = styled.div`
    position: sticky;
    top: 0;
    height: 100vh;
    margin-top: --100vh;
    text-align: center;

    display: flex;
    flex-direction: column;
    align-items: center;
`
